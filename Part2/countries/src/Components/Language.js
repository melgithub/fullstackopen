import React from 'react'

//Responsible for display of an individual language; used in the LanguageList component.

const Language = ({language}) => {
    return (
        <li>{language}</li>
    )
}

export default Language
